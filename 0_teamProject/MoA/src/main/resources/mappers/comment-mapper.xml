<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.og.moa.board.freeboard.model.dao.FreeBoardCommentMapper">

	<!-- Comment resultMap -->
	<resultMap type="Comment" id="comment_rm">
		<id property="commentNo" column="COMMENT_NO"/>
		<result property="memberNo" column="MEMBER_NO"/>
		<result property="boardNo" column="BOARD_NO"/>
		<result property="commentCreateDate" column="C_CREATE_DATE"/>
		<result property="commentContent" column="COMMENT_CONTENT"/>

		<result property="commentDeleteFlag" column="COMMENT_DEL_FL"/>
		<result property="parentNo" column="P_COMMENT"/>
		<result property="memberNickname" column="MEMBER_NICKNAME"/>
		<result property="profileImage" column="PROFILE_IMG"/>
	</resultMap>
	
	<!-- 댓글 목록 조회 (단일 구조) -->
	<select id="select" resultMap="comment_rm">
	    SELECT COMMENT_NO,
	           MEMBER_NO,
	           BOARD_NO,
	           TO_CHAR(C_CREATE_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분"') AS C_CREATE_DATE,
	           COMMENT_CONTENT,
	           MEMBER_NICKNAME,
	           PROFILE_IMG,
	           COMMENT_DEL_FL
	    FROM "COMMENT"
	    JOIN MEMBER USING(MEMBER_NO)
	    WHERE BOARD_NO = #{boardNo}
	      AND COMMENT_DEL_FL = 'N'
	    ORDER BY COMMENT_NO
	</select>

	
	
	<!-- 댓글 삽입 -->
	<insert id="insert" useGeneratedKeys="true">
	    <selectKey order="BEFORE" resultType="_int" keyProperty="commentNo">
	        SELECT SEQ_COMMENT_NO.NEXTVAL FROM DUAL
	    </selectKey>
	
	    INSERT INTO "COMMENT"
	        (COMMENT_NO, 
	        MEMBER_NO,
	        BOARD_NO,
	        C_CREATE_DATE,
	        COMMENT_CONTENT,
	        COMMENT_DEL_FL,
	        P_COMMENT)
	    VALUES
			(#{commentNo}, #{memberNo}, #{boardNo}, DEFAULT, #{commentContent}, DEFAULT, NULL)
	</insert>

	
	<!-- 댓글 삭제 -->
	<update id="delete">
		UPDATE "COMMENT" SET
		COMMENT_DEL_FL = 'Y'
		WHERE COMMENT_NO = ${commentNo}
	</update>
	
	<!-- 댓글 수정 -->
	<update id="update">
		UPDATE "COMMENT" SET
		COMMENT_CONTENT = #{commentContent}
		WHERE COMMENT_NO = ${commentNo}
	
	</update>
	
	
</mapper>
